# 複製成 .env 後填入實際金鑰。.env 已在 .gitignore，不會被 commit。
# 取得方式見 README §2.1

# P2 負責申請：tdx.transportdata.tw → 會員專區 → 建立應用程式
TDX_CLIENT_ID=
TDX_CLIENT_SECRET=

# P1 負責申請：Google Cloud → 啟用 Routes API → 建立 API key
# ★ 記得設定每日用量上限 1000 次
GOOGLE_MAPS_API_KEY=
